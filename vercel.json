{
  "builds": [
    {
      "src": "acortador/wsgi.py",
      "use": "@vercel/python",
      "config": {
        "maxLambdaSize": "15mb",
        "installCommand": "pip install -r requirements.txt && python manage.py collectstatic --noinput && python manage.py migrate --noinput"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "acortador/wsgi.py"
    }
  ]
}
